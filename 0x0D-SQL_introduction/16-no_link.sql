-- Listing all records in the table second_table with name value
-- Displaying the score and the name, ordered in descending order
SELECT `score`, `name` FROM `second_table` WHERE `name` != "" ORDER BY `score` DESC;
